if [ ! -d py-virtual-env ]; then
    python3 -m venv py-virtual-env
fi

source          py-virtual-env/bin/activate

# pip  install torch
# pip  install transformers
pip  install   -U sentence-transformers
# pip3 install    sentence_transformers
