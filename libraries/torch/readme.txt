At its core, PyTorch provides two main features:
    - An n-dimensional Tensor, similar to numpy's arrays, but can run on GPUs
    - Automatic differentiation for building and training neural networks

